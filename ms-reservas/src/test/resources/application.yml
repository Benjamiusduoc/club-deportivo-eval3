spring:
  datasource:
    url: jdbc:h2:mem:ms_reservas_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

ms:
  socios:
    url: http://localhost:8081
  actividades:
    url: http://localhost:8082
  instructores:
    url: http://localhost:8086
